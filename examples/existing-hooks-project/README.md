# Existing Hooks Project Example

This example is intended for testing installation into a project that already has Claude Code hooks. The installer should merge Self-Evolve hooks without removing existing commands.
